// "111000"

print(Number.isInteger(-1));
print(Number.isInteger(0));
print(Number.isInteger(1));

print(Number.isInteger(-1.1));
print(Number.isInteger(1.1));

// print(Number.isInteger(Infinity));
// print(Number.isInteger(-Infinity));

print(Number.isInteger(NaN));